{
  "name": "ripple-tank-simulator",
  "version": "1.0.0",
  "scripts": {
    "build:wasm": "cd backend && wasm-pack build --target web --out-dir ../frontend/src/pkg",
    "dev": "npm run build:wasm && cd frontend && npm run dev"
  }
}